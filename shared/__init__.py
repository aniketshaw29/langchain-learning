"""`shared` holds helpers used by multiple lessons.

It is imported as `from shared.llm_factory import ...` because the lessons are
run *as modules* from the project root:  python -m lessons.01_llm_basics
"""